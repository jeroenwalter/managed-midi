<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WpfMidiPlayer.LogViewer"
                    xmlns:logViewer="clr-namespace:WpfMidiPlayer.LogViewer">

    <DataTemplate DataType="{x:Type logViewer:LogEntry}">
        <Grid IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="Index" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Date" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Margin="5,0,5,0"
                       FontWeight="Bold"
                       Text="{Binding DateTime}" />

            <TextBlock Grid.Column="1"
                       Margin="0,0,2,0"
                       FontWeight="Bold"
                       Text="{Binding Index}" />

            <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap" />
        </Grid>
    </DataTemplate>
</ResourceDictionary>